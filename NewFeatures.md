* When changing an item, show possible leather items for that slot. Sort based on item level descending. It should also be possible to type something and it will search for the item. 
* Make it possible to import from a file, use a json format.
* Make it possible to export to a file, use a json format.
* Add a filter for T4, T5, T6, when enabled only show in the tier or below
* Compile into an exe or something that can easily de run on windows

